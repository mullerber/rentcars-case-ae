version: 2

sources:
  - name: raw
    description: "Dados brutos carregados localmente via dbt seed no DuckDB."
    schema: main

    tables:
      - name: raw_sessions
      - name: raw_searches
      - name: raw_bookings
      - name: raw_cancellations
      - name: raw_partners